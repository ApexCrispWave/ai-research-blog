# task-024 — Build CrispWave Budget Web App — Phase 1: Core scaffold + document ingestion

## Objective
Legacy task migrated without a full objective yet.

## Scope
- Legacy task; scope needs cleanup if this task is reactivated

## Acceptance Criteria
- [ ] Spec needs explicit completion criteria

## Constraints / Requirements
- Preserve historical task context

## Context / Handoff
Phase 1 complete. Fixed next.config.js + better-sqlite3 Node 22 fix. Running at localhost:3001 with KeepAlive LaunchAgent.

## Verification
http://localhost:3001 serves budget app; file upload works for PDFs and CSVs
