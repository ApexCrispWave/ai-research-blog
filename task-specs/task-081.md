# task-081 — Dashboard: Animated Terminal Dashboard (Pure CSS)

## Objective
Build a new dashboard page at /office-terminal with a Matrix/hacker aesthetic. Glowing green terminals in a dark room. Each agent has a terminal screen showing live task with typing animations, progress bars, blinking cursors. Tech: Pure CSS + React components on Next.js page. Lighter weight than canvas options.

## Scope
- Legacy task; scope needs cleanup if this task is reactivated

## Acceptance Criteria
- [ ] Spec needs explicit completion criteria

## Constraints / Requirements
- Preserve historical task context

## Context / Handoff
Animated terminal-style dashboard (pure CSS) created at /dashboard/terminal-dashboard.html. Features: glowing green terminals, scrolling logs, status indicators, model activity visualization, retro terminal colors, typing animations, blinking cursors, progress bars. Responsive design, production-ready.

## Verification
Verification not yet standardized.
